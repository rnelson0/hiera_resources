---
test1:
  notify:
    title 1:
      message: this is the first message stored in YAML
test2:
  notify:
    title 2: {}
test3:
  notify:
    title 3:
test defaults:
  notify:
    title defaults: {}
test defaults 2:
  notify:
    title defaults:
      message: this is not the default message
